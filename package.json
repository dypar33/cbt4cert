{
  "name": "cbt4cert",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:deploy": "npm run build && npm run copy-assets",
    "copy-assets": "cp dist/index.html . && cp -r dist/assets . && echo 'Assets copied for GitHub Pages deployment'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
